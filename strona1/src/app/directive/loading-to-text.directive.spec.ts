import { LoadingToTextDirective } from './loading-to-text.directive';

describe('LoadingToTextDirective', () => {
  it('should create an instance', () => {
    const directive = new LoadingToTextDirective();
    expect(directive).toBeTruthy();
  });
});
